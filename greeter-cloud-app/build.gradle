mainClassName = 'com.sl.happylife.greetercloud.GreeterCloudApplication'

dependencies {
    compile "org.springframework.cloud:spring-cloud-config-client:${springCloudVersion}"
    compile project(':greeter-schedule')
}